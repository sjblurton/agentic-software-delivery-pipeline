import { BootstrapStatus } from "@/features/bootstrap/components/bootstrap-status";

export default function Home() {
  return (
    <main className="mx-auto flex w-full max-w-4xl flex-1 p-6 md:p-10">
      <BootstrapStatus />
    </main>
  );
}
